# slidev-pptx

Slidev で書いたスライドを、PowerPoint 上で修正できる PPTX として書き出すための語彙。
書き出しの向きは一方向（`slides.md` が原本、PowerPoint は最後の手直し）。

## Language

**PPT 部品**:
PowerPoint に対応する要素（テキスト・図形・画像・表など）を持つ Vue コンポーネント。`<PptText>` のように `Ppt` を接頭辞にする。
_Avoid_: 挿入部品、パワポ部品、コンポーネント（単独で）

**ネイティブ書き出し**:
スライドの中身を、PowerPoint で編集できる図形・表・テキスト・プレースホルダーとして PPTX に出すこと。
_Avoid_: XML 書き出し、編集可能エクスポート

**画像への置き換え**:
ネイティブ書き出しできない中身を、画像にして PPTX に貼ること。置き換えた部分は PowerPoint で編集できない。
_Avoid_: ラスタライズ、スクリーンショット貼り付け

**画像書き出し**:
Slidev 標準の PPTX 書き出し。各スライド全体を 1 枚の画像として貼る。ネイティブ書き出しとは別の経路。
_Avoid_: 従来の書き出し、標準エクスポート

**レイアウト対応表**:
Slidev のレイアウト名（`cover` `default` `center` `two-cols` など）と、PPTX のスライドマスターのレイアウトおよびプレースホルダーの対応。表にないレイアウトは位置指定のテキスト枠で出す。
_Avoid_: マッピング、テンプレート表

**実測配置**:
ブラウザで描画した PPT 部品の位置と大きさを測り、それを PPTX の座標に換算して置くこと。既定の配置方法。
_Avoid_: 自動配置、DOM 計測

**座標指定**:
PPT 部品に Slidev の画面の px で位置と大きさを書き、実測配置の代わりに使うこと。
_Avoid_: 絶対配置、固定配置

**収集結果**:
ブラウザで測った、スライドごとの要素（テキスト枠・図形・線・画像・表）の列。単位は Slidev の画面の px で、レイアウト名やノートは含まない。
_Avoid_: Capture（コードの中だけ）、DOM ダンプ、スナップショット

**後処理**:
PptxGenJS が書き出した ZIP の中の XML に対する、パス指定の変換の列。PptxGenJS が出せないもの・壊すものをここで直す。
_Avoid_: パッチ、ポストプロセス、XML 修正

**OPC 整合チェッカ**:
PPTX の ZIP・rels・`[Content_Types].xml`・図形 id の整合を点検する検査。PowerPoint の「修復」に掛かる原因を CI で止める。
_Avoid_: バリデータ、lint、構造検査（単独で）

**書き出しの記録**:
画像への置き換え一覧・警告・規則として捨てたものの件数を、標準出力と JSON に出したもの。
_Avoid_: ログ、レポート、サマリ

**流し込みブロック / 区切りブロック**:
1 つのテキスト枠にまとめて流す要素（見出し・段落・箇条書き）と、枠を閉じる要素（表・コード・引用・画像・置き換え・PPT 部品・装飾つきの箱）。
_Avoid_: インライン / ブロック（HTML の意味と混ざる）、フロー要素
