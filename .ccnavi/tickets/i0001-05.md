---
version: 1
ticket: i0001-05
parent: i0001
phase: 5
predecessors:
- i0001-04
human_review:
  required: false
  reason: 文書だけ。コードにも振る舞いにも触れない
title: README にネイティブ書き出しの手順を書き、用語と ADR を揃える
rationale: '実装（i0001-04）で `export:pptx` の意味が変わり、部品と CLI が増えた。利用者が最初に読む README に

  手順・前提・出せないものを書く。設計で決めた用語を CONTEXT.md に足し、実装で分かった制約を ADR の

  Consequences に写す。

  '
allow:
- match: Write|Edit
  glob: README.md
- match: Write|Edit
  glob: docs/*
- match: Write|Edit
  glob: CONTEXT.md
started_at: 2026-09-13T23:29:14+0900
completed_at: ''
base_sha: 4b97d2404b4d952273ff138f8974277d8f4fb804
ccnavi_approved:
  approved_at: 2026-09-13T23:28:23+0900
  source_tree: i0001
  source_path: /Volumes/Data/git/slidev-pptx/.claude/worktrees/i0001/wip/tickets/todo/i0001-05.md
---
## 成果物

1. `README.md`
   - 何ができるか（Markdown の見出し・段落・箇条書き・表・コードはネイティブ、数式・Mermaid・その他の HTML は画像）と、向きが一方向であること
   - 手順: `pnpm install` → `pnpm export:pptx`（`slides-export.pptx` と `slides-export.pptx.report.json`）。`export:pptx-image` は画像書き出し
   - CLI の引数（`--output` `--range` `--lang` `--wait` `--timeout` `--report` `--no-check` `--strict` `--keep-server`）と exit code
   - 前提: Node 22.18 以降、`playwright-chromium`、Windows の PowerPoint で確かめる。フォント（游ゴシック / Consolas。Mac は 游ゴシック体 / Menlo に落ちる）
   - PPT 部品 4 種の使い方（props の要点と `slides.md` のデモ）
   - 出せないもの・警告コードの一覧（`W-*` の意味と対処）、`--strict` の使いどころ
   - 検査の回し方（`pnpm test` `pnpm test:e2e`）
2. `CONTEXT.md` — 設計 native-export.md §12 の用語（収集結果、後処理、OPC 整合チェッカ、書き出しの記録、
   流し込みブロック / 区切りブロック）を既存の形式で追加
3. `docs/adr/0001-pptxgenjs-with-xml-patch.md` — Consequences に、実装で分かった PptxGenJS の制約
   （placeholder 指定で options が総取りされる、`addText` の要素リンクは rels に載らない、`margin` の並びが型定義と違う、
   SVG 経路が壊れる）を写す

## 決め方

- README は利用者向け。設計文書の写しにしない。手順は上から順に打てば動く形で
- `wip/design/*` の更新（`tests/README.md` の「設計へ書き戻す候補」）はこのチケットの範囲外。親の `feedback:` に
  design-feedback を並べる改版を利用者に依頼する

## 範囲外

コード、検査、設計文書、`slides.md`。
