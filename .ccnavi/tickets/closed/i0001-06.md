---
version: 1
ticket: i0001-06
parent: i0001
phase: 6
predecessors:
- i0001-04
human_review:
  required: true
  reason: 設計文書の変更。実装が先に決めた振る舞いを設計に反映するので、設計と実装の食い違いが残らないかを人が見る
title: 実装で分かった実物との差を設計文書に書き戻す（design-feedback）
rationale: '実装（i0001-04）で、設計の記述が Slidev / PptxGenJS / xmldom の実物と食い違っていた箇所と、設計に無かった判断が

  `tests/README.md` の「設計へ書き戻す候補」（約 35 件）に溜まっている。設計文書が原本なので、そこへ反映して

  検査・実装・設計の 3 つを揃える。

  '
allow:
- match: Write|Edit
  glob: wip/design/*
- match: Write|Edit
  glob: docs/*
started_at: 2026-09-14T00:03:53+0900
completed_at: 2026-09-14T00:25:32+0900
base_sha: bb38d52699b54f60aa9255c1bfbc370a5421c041
ccnavi_approved:
  approved_at: 2026-09-14T00:02:49+0900
  source_tree: i0001
  source_path: /Volumes/Data/git/slidev-pptx/.claude/worktrees/i0001/wip/tickets/todo/i0001-06.md
---
## 成果物

`tests/README.md` の「設計へ書き戻す候補」と「検査が前提にしている引数の形と、設計に無い細部」を 1 件ずつ設計に反映する。
反映した候補は README の一覧から消す（README は `tests/*` なのでこのチケットの範囲外。消すのは実装フェーズの子か、
次の実装フィードバックで）。

1. `wip/design/ppt-components.md`
   - §2.1: 根の列。根が `.slidev-layout` のときも根自身の `background-image`（`layout: image`）と背景色（`layout: end`、`layoutClass`）を読む。
     `two-cols-header` の `.col-header` / `.col-bottom` は根の領域、`.col-left` / `.col-right` だけ副領域
   - §2.2: 規則 12 のタグ一覧に `a`。「`p` の中身が `img` / `.katex-display` / `svg` / `.mermaid` だけなら `p` ごと」の特例。
     規則 1 は `hr`（高さ 0〜1 px）を除外しない。段落の中の svg / img / table は `W-INLINE` を出して無視する（枠の単位でしか置き換えられない）
   - §3.4: `[x](#3)` は Slidev が `href="##3"` にするので `##N` も内部リンク。相対リンク・数字でないアンカーは外して `W-LINK`。
     空白の畳み込みは CSS と同じ `[ \t\r\n\f]` だけ。読めない色（oklch、`transparent`）は黒にして `W-CSS`。`<u>` が `<a>` の中なら下線は本物。
     `transparency = 100 − opacity × 100` は整数に丸める
   - §3.5: 罫線はセルが 0 なら `tr` → `table` へ遡る。`caption` は表の前の段落。§3.6: `.slidev-code-dishonored` の opacity は無視する
   - §1.3 / §1.5: `PptShape` の `align` / `valign` の既定は `center` / `middle`、`padding` prop、SVG の形は近似、`type="line"` の slot は描かない。
     `PptTable` の `header: false` は slot の表の `thead` を消せない。PPT 部品と `zoom` の組み合わせは未定義（明記する）
2. `wip/design/native-export.md`
   - §1.2: UnoCSS の生成の確認（2 信号、再読み込み最大 3 回、`W-RENDER`）。`NODE_ENV=development` で立てる。Vite の依存最適化の再読み込みを待つ
   - §3.2: 後処理 5 の区切りは `\r\n | \r | \n`（DOM では LF）。§3.3: 縮小率は 2 条件の不足率の大きいほう、5% を超えるときだけ
   - §4.1: 線は端点をキャンバスの中に寄せ、どちらかの軸で全体が外なら除外する。§4.2: 同じ `roleHint` の枠は 2 つ目以降を自由配置に
   - §4.3: 表の `headerRows` は出さない。`rowH` の比例縮小。SVG の `<img>` は撮影に回す。§4.4: ノートの run は `lang` が効かない
   - §7: `--range` は Node 側でも絞る。`--keep-server` は失敗時だけ。数値フラグと `--range` の綴りが不正なら exit 2
   - §8: `Report.slideMap`、`dropped['background-crop' | 'console-noise']`、警告コード `W-LINK`（2 義）`W-INLINE` `W-RENDER`（3 義）。
     `W-TRANSITION` を警告に出すか `dropped` だけにするかを 1 つに決める（今は両方）
   - §5.2 の「失うもの」に placeholder の固定位置と下の自由配置の要素の重なりを足す
   - §11: 要確認の答え（`tests/README.md` の「要確認への答え」）を書く。3 は利用者が PowerPoint 実機で確認済み（2026-09-13）
3. `docs/adr/0001-pptxgenjs-with-xml-patch.md` — i0001-05 で足した Consequences と設計の記述が矛盾しないことを確かめる

## 決め方

- 設計の文面を直すときは「なぜ実物と違ったか」（実測の根拠）を 1 行添える。`tests/README.md` に書いてある
- 調査記録 `wip/research/summary.md` §3 の「DOM の class から読むほうが確実」の訂正は範囲外。`wip/research/*` は親の allow にあるので、親で直すか利用者に依頼する

## 範囲外

コード、検査、README、CONTEXT.md、`wip/research/*`。
