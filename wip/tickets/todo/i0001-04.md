---
version: 1
ticket: i0001-04
parent: i0001
phase: 4
predecessors: [i0001-02, i0001-03]
human_review:
  required: true
  reason: 外から見える振る舞い（export:pptx の意味、slides.md のフォント）が変わり、依存が増える
title: ネイティブ書き出しを実装して受入テストを通す
rationale: |
  設計（i0001-02）と受入テスト（i0001-03）に従って `packages/slidev-addon-pptx` を実装する。
  完了条件は `tests/vitest.config.ts` の検査が全部通り、`tests/vitest.e2e.config.ts` の検査が
  Playwright の環境で通ること。設計に無い判断が要る箇所は、実装で決めずに design-feedback の子を切る。
allow:
  - match: Write|Edit
    glob: "packages/*"
  - match: Write|Edit
    glob: "components/*"
  - match: Write|Edit
    glob: "tests/*"
  - match: Write|Edit
    glob: "slides.md"
ask:
  - match: Write|Edit
    glob: "package.json"
  - match: Write|Edit
    glob: "pnpm-lock.yaml"
  - match: Write|Edit
    glob: "pnpm-workspace.yaml"
started_at: ""
completed_at: ""
base_sha: ""
---
## 成果物

順番は「依存 → 単体で通る層 → e2e で通る層 → デッキ側」。各段でコミットを分ける。

1. **依存と置き場**（最初に。ここが入れば i0001-03 の検査が回せる）
   - `pnpm-workspace.yaml` に `packages: [packages/*]`
   - ルート `package.json`: devDependencies に `vitest` `@xmldom/xmldom` `jszip` `pptxgenjs`、
     `"slidev-addon-pptx": "workspace:*"`、scripts に `test` `test:e2e`（`tests/README.md` の 2 行）、
     `export:pptx` を `slidev-pptx export slides.md` に、`export:pptx-image` を `slidev export --format pptx` に
   - `packages/slidev-addon-pptx/package.json`（name、bin `slidev-pptx`、Slidev アドオンの設定）
2. **ブラウザ無しで通る層**（`pnpm test` が緑）
   - `src/types.ts`、`src/build/{units,names,sanitize,fonts,notes,layout,masters,convert}.ts`、
     `src/patch/{zip,index}.ts` と `patches/*.ts`（後処理 1〜7）、`src/opc/check.ts`（C1〜C14）
   - 実装の順は検査の依存順: units → sanitize → names → fonts → notes → layout → masters → patch/zip →
     patches → opc/check → convert
3. **e2e で通る層**（`pnpm test:e2e` が緑。playwright-chromium が要る）
   - `src/collect/index.ts`（`collect` を 1 関数に閉じる。`page.evaluate` に渡す）
   - `src/export.ts`（§1 の流れ。画像の取得、置き換えの撮影、記録の書き出し）
   - `src/cli.ts`（§7）
4. **PPT 部品とデッキ**
   - `components/Ppt{Text,Shape,Image,Table}.vue`（ppt-components.md §1）
   - `tests/e2e/components.test.ts` の `describe.skip` を外して通す
   - `slides.md` に `addons: [slidev-addon-pptx]` と `fonts: { sans: '游ゴシック', mono: 'Consolas', local: [...] }`、
     PPT 部品を使うスライドを 1 枚
5. **設計の「要確認」への答え**（native-export.md §11 の 1〜7）を、検査の結果として `tests/README.md` に 1 行ずつ書く
   （設計文書への書き戻しは design-feedback の子）

## 決め方

- 検査を緩めない。検査が設計と食い違っていたら、設計の側を正すか検査を直すかを design-feedback で決める
- 設計に無い判断（`tests/README.md` の「設計へ書き戻す候補」: `p` の中身が `img` だけの特例、`hr` の高さ 0）は、
  実装で先に決めて `tests/README.md` に書き、design-feedback の子で設計に写す
- PowerPoint 実機（Windows）での開封は人のレビューで。修復が出たら OPC 検査に規則を足す
- `node` は PATH に無い環境がある。scripts は `pnpm` 経由で書く

## 範囲外

README（フェーズ 5）、CONTEXT.md の用語追加、設計文書の更新、既存テンプレートへのマスター差し替え、アニメーション。
